apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: bookstack
  labels:
    service: nginx-claim0
  name: nginx-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
